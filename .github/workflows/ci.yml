name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install build tools
      run: sudo apt-get update; sudo apt-get install -y build-essential make

    - name: Build and run tests
      run: make -C challenge07 test
